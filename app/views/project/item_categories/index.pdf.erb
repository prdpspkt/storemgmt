<table class="table table-wide black-border">
  <thead>
  <tr>
    <td>क्र.स.</td>
    <td>ID</td>
    <td>वर्गीकरणको नाम </td>
    <td>वर्गीकरणको नाम (अंग्रेजी) </td>
    <td>कैफियत</td>
  </tr>
  </thead>
  <tbody>
  <% @sn = 1 %>
  <% @item_categories.each do |cat| %>
    <tr>
      <td><%= nd @sn %></td>
      <td><%= cat.id %></td>
      <td><%= cat.name_ne %></td>
      <td><%= cat.name_en %></td>
      <td></td>
    </tr>
    <% @sn = @sn + 1 %>
  <% end %>
  </tbody>

</table>