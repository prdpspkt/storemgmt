<% @items.each do |item| %>
  <% if item.item_transactions.where(fiscal_year_id: @fiscal_year.id).count > 0 %>
    <%= render "project/ledgers/non_expense_item_register", item: item %>
  <% end %>
<% end %>