<%= form_with(model: repair_record_register, local: true) do |form| %>
  <% if repair_record_register.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(repair_record_register.errors.count, "error") %> prohibited this repair_record_register from being saved:</h2>

      <ul>
      <% repair_record_register.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">

  <div class="col-md-6">
    <%= form.label :name_of_item %>
    <%= form.select(:item_transaction_id, Office::ItemTransaction.where(item_classification_no: 408).where(transaction_type: 1).collect{ |item| [item.item.name_of_item_en, item.id]}, {include_blank: "choose"}, {class: "form-control select2", required: :required})  %>
  </div>

  <div class="col-md-6">
    <%= form.label :page_no %>
    <%= form.text_field :page_no, class: "form-control", readonly: true %>
  </div>
  </div>
<div class="separator"></div>
  <div class="float-right">
    <%= form.submit :save, class: "btn btn-success" %>
  </div>
<% end %>
