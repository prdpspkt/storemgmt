<%= form_with(model:committee, local: true) do |form| %>
  <% if committee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(committee.errors.count, "error") %> prohibited this committee committee from being saved:</h2>

      <ul>
      <% committee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="col-4">
    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name, class: "form-control preeti", required: true %>
    </div>
  </div>
  <div class="col-4">
    <div class="field">
      <%= form.label :committee_formation_date %>
      <%= form.text_field :committee_formation_date, class: "form-control date-picker", required: true %>
    </div>
  </div>
  <div class="col-4">
    <div class="field">
      <%= form.label :report_submission_date %>
      <%= form.text_field :report_submission_date, class: "form-control date-picker", required: true %>
    </div>
  </div>
</div>
  <div class="separator"></div>
    <div class="float-right">
    <%= form.submit :save, class: " btn btn-success " %>
  </div>
<% end %>
