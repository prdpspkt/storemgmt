<%= form_with(model: fiscal_year, local: true) do |form| %>
  <% if fiscal_year.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fiscal_year.errors.count, "error") %> prohibited this fiscal_year from being saved:</h2>

      <ul>
      <% fiscal_year.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="col-sm-6 col-lg-4">
    <%= form.label "आर्थिक वर्ष" %>
    <%= form.text_field :fy, class: "form-control" %>
  </div>
  <div class="col-sm-6  col-lg-4">
    <%= form.label "सुरुको दिन" %>
    <%= form.text_field :start_date, class: "form-control date-picker" %>
  </div>
  <div class="col-sm-6 col-lg-4">
    <%= form.label "बन्द हुने दिन" %>
    <%= form.text_field :closing_date, class: "date-picker form-control" %>
  </div>
</div>
  <div class="separator"></div>
   <div class="form-group">
    <%= form.submit :save, class: "btn btn-success float-right" %>
  </div>
<% end %>
