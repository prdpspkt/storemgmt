<%= form_with(model: item_assistance_register_item, local: true) do |form| %>
  <% if item_assistance_register_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_assistance_register_item.errors.count, "error") %> prohibited this item_assistance_register_item from being saved:</h2>

      <ul>
      <% item_assistance_register_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.hidden_field :returned, value: true %>
<div class="row">
  <div class="col-6">
    <%= form.label :returned_by %>
    <%= form.text_field :returned_by, class: "form-control preeti", required: true %>
  </div>
  <div class="col-6">
    <%= form.label :returned_quantity %>
    <%= form.number_field :returned_quantity, step: :any, class: "form-control", required: true %>
  </div>

  <div class="col-6">
    <%= form.label :returned_date %>
    <%= form.text_field :returned_date, class: "form-control date-picker", required: true, readonly:true %>
  </div>

</div>
<div class="separator"></div>
  <div class="float-right">
    <%= form.submit :save, class: "btn btn-success" %>
  </div>
<% end %>
