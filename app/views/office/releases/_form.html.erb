<%= form_with(model: office_release, local: true) do |form| %>
  <% if office_release.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(office_release.errors.count, "error") %> prohibited this office_release from being saved:</h2>

      <ul>
      <% office_release.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
  <div class="col-sm-6">
    <%= form.label :received_by %>
    <%= form.text_field :received_by, class: "form-control preeti" %>
  </div>
   <div class="col-sm-6">
    <%= form.label :store_keeper_signed_date %>
    <%= form.text_field :store_keeper_signed_date, class: "form-control date-picker", readOnly: true, required: true %>
  </div>


  <div class="col-sm-6">
    <%= form.label :office_chief_signed_date %>
    <%= form.text_field :office_chief_signed_date, class: "form-control date-picker",  readOnly: true, required: true %>
  </div>
</div>
  <div class="separator"></div>
  <div class="actions">
    <%= form.submit :save, class: "btn btn-success float-right" %>
  </div>
<% end %>
